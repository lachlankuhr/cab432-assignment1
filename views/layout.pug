doctype html
html
    head
        block head
        block links
    body 
        #container
            block search
            .row.content
                #sidebar.col-lg-3.col-sm-12(scrolltop='10')
                    .row 
                        h1 Events
                    each event in events
                        +drawevent(event)
                #map.col-lg-9.col-sm-12
                    #buttonOverlay
                    block map
            block footer

